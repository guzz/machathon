Run the `setup.sh` script to initiate the repositories.

Run `yarn dev` to start all applications.

#### Applications

- Chat interface - http://localhost:3000
- Auth Notify - http://localhost:3031
- Main Api - http://localhost:3030